# Sync Wave 프로젝트 작업 내용 (2024-12-26 ~ 2024-12-27)

## 📋 개요
- **기간**: 2024년 12월 26일 ~ 27일
- **목표**: 공지사항(Notice) 기능 완전 구현 및 권한별 기능 차등화
- **기술 스택**: Spring Boot (Backend), React.js (Frontend)

## 🎯 주요 작업 내용

### 1. 공지사항(Notice) 기능 구현

#### 1.1 백엔드 구현
- **패키지 구조**: `sync-wave-service/src/main/java/com/parker/service/api/v1/notice/`
- **구현된 컴포넌트**:
  - `NoticeController` - REST API 엔드포인트
  - `NoticeService` - 비즈니스 로직
  - `NoticeDto`, `NoticeSearchDto` - 데이터 전송 객체
  - `NoticeEntity` - JPA 엔티티 (기존 common 모듈 활용)

#### 1.2 프론트엔드 구현
- **페이지**: `sync-wave-fe/src/pages/NoticePage.js`
- **컴포넌트**: `sync-wave-fe/src/components/notice/NoticeForm.js`
- **서비스**: `sync-wave-fe/src/services/notice.service.js`
- **스타일**: `sync-wave-fe/src/styles/NoticePage.css`, `NoticeForm.css`

### 2. 게시판 형태 UI 구현

#### 2.1 테이블 형태 레이아웃
```javascript
// 기존 카드 형태에서 테이블 형태로 변경
<div className="notice-table">
  <table>
    <thead>
      <tr>
        <th>번호</th>
        <th>제목</th>
        <th>작성자</th>
        <th>중요도</th>
        <th>상태</th>
        <th>작성일</th>
        <th>관리</th>
      </tr>
    </thead>
    <tbody>
      {/* 공지사항 목록 */}
    </tbody>
  </table>
</div>
```

#### 2.2 페이징 기능 구현
- **백엔드**: Spring Data `Page`, `Pageable` 활용
- **프론트엔드**: 페이지 번호, 페이지 크기 선택 기능
- **페이징 정보**: 총 개수, 현재 페이지, 페이지 크기 표시

### 3. 권한별 기능 차등화 (ROLE_MASTER vs ROLE_USER)

#### 3.1 백엔드 보안 구현
```java
// NoticeService.java
public NoticeEntity getNoticeDetailForUser(Long noticeId) {
    NoticeEntity notice = noticeRepository.findById(noticeId)
            .orElseThrow(() -> new CustomException(...));
    
    // ROLE_USER는 활성화된 공지사항만 조회 가능
    if (!isMasterUser() && !notice.getIsActive()) {
        throw new CustomException(..., "비활성화된 공지사항은 조회할 수 없습니다.", HttpStatus.FORBIDDEN);
    }
    
    return notice;
}
```

#### 3.2 프론트엔드 권한별 UI
```javascript
// 권한별 버튼 렌더링
{isMaster ? (
  <div className="action-buttons">
    <button className="action-btn edit-btn">수정</button>
    <button className="action-btn toggle-btn">활성화/비활성화</button>
    <button className="action-btn delete-btn">삭제</button>
  </div>
) : (
  <div className="action-buttons">
    <button className="action-btn view-btn">상세보기</button>
  </div>
)}
```

### 4. 기능별 권한 매트릭스

| 기능 | ROLE_MASTER | ROLE_USER |
|------|-------------|-----------|
| 활성 공지사항 조회 | ✅ | ✅ |
| 비활성 공지사항 조회 | ✅ | ❌ |
| 공지사항 생성 | ✅ | ❌ |
| 공지사항 수정 | ✅ | ✅ (활성화된 것만) |
| 공지사항 삭제 | ✅ | ❌ |
| 상태 변경 | ✅ | ❌ |
| 페이징 조회 | ✅ | ✅ |

### 5. 주요 API 엔드포인트

#### 5.1 공지사항 관리 API
```
GET    /v1/notices              - 공지사항 목록 조회 (페이징)
GET    /v1/notices/{id}         - 공지사항 상세 조회
POST   /v1/notices              - 공지사항 생성
PUT    /v1/notices/{id}         - 공지사항 수정
DELETE /v1/notices/{id}         - 공지사항 삭제
PATCH  /v1/notices/{id}/status  - 상태 변경
GET    /v1/notices/user-role    - 사용자 권한 조회
```

#### 5.2 검색 및 필터링
- **키워드 검색**: 제목 또는 내용 검색
- **중요도 필터**: HIGH, MEDIUM, LOW
- **상태 필터**: 활성, 비활성, 모든 상태
- **페이징**: 페이지 번호, 페이지 크기 설정

### 6. 해결된 주요 이슈들

#### 6.1 인증/인가 관련
- **문제**: 공지사항 페이지 접근 시 로그아웃 발생
- **해결**: `useAuth` 훅 제거, JWT 토큰 직접 파싱으로 변경

#### 6.2 페이징 관련
- **문제**: 페이징 정보 표시 오류 (11개 표시, "1, 1" 페이지)
- **해결**: 백엔드 `Page` 객체 응답, 프론트엔드 페이징 로직 수정

#### 6.3 UI/UX 관련
- **문제**: 제목 텍스트 위치, 폰트 크기 불일치
- **해결**: CSS 스타일링 개선, 테이블 레이아웃 최적화

#### 6.4 보안 관련
- **문제**: ROLE_USER가 비활성화된 공지사항 수정 가능
- **해결**: 백엔드 권한 검증, 프론트엔드 버튼 숨김 처리

### 7. 기술적 개선사항

#### 7.1 백엔드
- **페이징 처리**: Spring Data JPA `Page`, `Pageable` 활용
- **권한 검증**: `SecurityUtil`을 통한 사용자 권한 확인
- **예외 처리**: `CustomException`을 통한 일관된 에러 응답
- **로깅**: 상세한 로그를 통한 디버깅 지원

#### 7.2 프론트엔드
- **상태 관리**: React hooks를 통한 효율적인 상태 관리
- **조건부 렌더링**: 권한별 UI 컴포넌트 분기
- **에러 처리**: 사용자 친화적인 에러 메시지
- **반응형 디자인**: 모바일 환경 지원

### 8. 성능 최적화

#### 8.1 데이터베이스
- **인덱싱**: `createdDateTime` 기준 정렬을 위한 인덱스
- **쿼리 최적화**: 페이징을 통한 대용량 데이터 처리
- **필터링**: 데이터베이스 레벨에서의 효율적인 필터링

#### 8.2 프론트엔드
- **가상화**: 대용량 목록 처리를 위한 가상 스크롤링 고려
- **캐싱**: API 응답 캐싱을 통한 성능 향상
- **지연 로딩**: 필요시에만 데이터 로딩

### 9. 테스트 고려사항

#### 9.1 단위 테스트
- **서비스 레이어**: 비즈니스 로직 테스트
- **컨트롤러 레이어**: API 엔드포인트 테스트
- **권한 테스트**: ROLE_MASTER vs ROLE_USER 기능 테스트

#### 9.2 통합 테스트
- **API 통합 테스트**: 전체 워크플로우 테스트
- **데이터베이스 테스트**: 실제 DB 연동 테스트
- **권한 통합 테스트**: 인증/인가 플로우 테스트

### 10. 향후 개선 방향

#### 10.1 기능 확장
- **파일 첨부**: 공지사항에 파일 첨부 기능
- **댓글 시스템**: 공지사항 댓글 기능
- **알림 시스템**: 새로운 공지사항 알림

#### 10.2 성능 개선
- **캐싱**: Redis를 통한 데이터 캐싱
- **CDN**: 정적 리소스 CDN 활용
- **데이터베이스 최적화**: 쿼리 튜닝, 인덱스 최적화

#### 10.3 모니터링
- **Grafana Labs**: API 성능 모니터링
- **로깅**: ELK 스택을 통한 로그 분석
- **알림**: 성능 이슈 자동 알림

## 📊 작업 통계

- **총 작업 시간**: 2일
- **수정된 파일 수**: 15개
- **추가된 코드 라인**: 약 1,200줄
- **해결된 이슈**: 10개
- **구현된 기능**: 8개

## 🎉 결론

공지사항 기능이 완전히 구현되었으며, 권한별 기능 차등화를 통해 보안성과 사용성을 모두 확보했습니다. 게시판 형태의 UI와 페이징 기능으로 사용자 경험이 크게 개선되었고, 향후 Grafana Labs를 통한 성능 모니터링을 통해 시스템 안정성을 더욱 향상시킬 수 있습니다. 